data_dir: '/content/'
use_sample: True
use_glove: True
glove_dir: '/content/'
glove_dim: 300
# qtl_src_len: 0.98
max_src_len: 200
use_cuda: True
bsz: 16
lr: 0.0001
num_workers: 2
model_dir: 'sample_simple_lr_00001'
exp_root: '/content/drive/My Drive/U/2019-1/DL/proyecto/'
epochs: 15
model:
  d_model: 300
  nhead: 10
  num_encoder_layers: 4
  num_decoder_layers: 4
  dim_feedforward: 512
  dropout: 0.1

